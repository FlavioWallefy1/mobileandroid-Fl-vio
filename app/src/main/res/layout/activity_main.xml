<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Pressione um botão para começar a tarefa assíncrona."
        android:textSize="18sp"
        android:textAlignment="center"
        android:padding="24dp"
        android:background="#E0F7FA"
        android:layout_marginBottom="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/btnSimulateApi"
        app:layout_constraintVertical_chainStyle="packed"/>

    <Button
        android:id="@+id/btnSimulateApi"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Simular Chamada API (Launch + IO)"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/tvStatus"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <Button
        android:id="@+id/btnSimulateCalculation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Simular Cálculo Paralelo (Async + Await)"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@+id/btnSimulateApi"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
